#include "main.h"

/**
 * _abs - check for absolute integer
 * @n: number to be checked
 * Return: 1 if positive, 0 if negative
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

	return (0);
}
