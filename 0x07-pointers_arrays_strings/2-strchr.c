#include <string.h>

/**
 * _strchr - print the first occurrance of a character
 * @s: character to check for occurrance
 * @c: character to search for
 * Return: the matched character
 */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
