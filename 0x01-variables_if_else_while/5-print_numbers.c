#include <stdio.h>

/**
 * main - print alphabet in lower case
 * Return: it return nothing
 */

int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		printf("%i", n);
		n++;
	}

	putchar(10);

	return (0);
}
